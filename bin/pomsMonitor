#!/usr/bin/env python3
"""Analyze POMS jobdesc JSON files."""

import os
import sys

utils_dir = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), 'utils')
sys.path.insert(0, utils_dir)

from pomsMonitor import main

if __name__ == '__main__':
    main()

