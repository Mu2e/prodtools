#!/bin/bash

# Wrapper for utils/runmu2e.py: execute production jobs from job definitions.
SCRIPT_DIR="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"

source /cvmfs/mu2e.opensciencegrid.org/setupmu2e-art.sh
muse setup ops
setup OfflineOps

exec python3 "$SCRIPT_DIR/../utils/runmu2e.py" "$@"
