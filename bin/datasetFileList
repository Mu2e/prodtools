#!/bin/bash
# Same interface and behavior as the original Perl mu2eDatasetFileList.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/../utils/datasetFileList.py" "$@"